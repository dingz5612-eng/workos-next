# Accommodation.CheckIn

Owns application, stay order, deposit, finance confirmation, and check-in runtime behavior.

Current status: runtime skeleton. Next step is aggregate persistence for `Application`, `StayOrder`, `Deposit`, `FinanceConfirmation`, and `CheckInRecord`.
