name: workosnext-local

services:
  postgres:
    image: postgres:16
    container_name: workosnext-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: workosnext
      POSTGRES_USER: workosnext
      POSTGRES_PASSWORD: workosnext_dev
    ports:
      - "54329:5432"
    volumes:
      - workosnext-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U workosnext -d workosnext"]
      interval: 10s
      timeout: 5s
      retries: 5
    labels:
      oam.workosnext.role: "local-postgres-runtime"
      oam.workosnext.boundary: "local-development-only"

volumes:
  workosnext-postgres:
    labels:
      oam.workosnext.role: "local-postgres-data"
