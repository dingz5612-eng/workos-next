version: 2
generatedBy: oam-cab-v1-final
components:
  - id: ArchitectureAuthority
    name: Architecture Authority
    path: docs/engineering/00-rule-authority.md
    status: active
    owner: architecture
    owner_domain: architecture
    responsibility: Highest engineering rule authority and OAM-CAB execution boundary.
    reason: OAM-CAB must follow Rule Authority, acceptance rules, current-state, API boundary, fact ownership, and MR contract before implementation.
    allowed_usage: Decide rule precedence, document architecture boundaries, and stop execution when evidence or authority conflicts.
    forbidden_usage: Override V5.5 Rule Authority, current-state, API boundary, fact ownership, MR contract, or release evidence.
    evidence:
      - docs/engineering/00-rule-authority.md
      - docs/rules/v5.5/rule-authority.yml
      - docs/acceptance/13-v5.5-rules-os-go-no-go.md
      - scripts/check-rule-authority.mjs
  - id: AdmissionKernel
    name: Admission Kernel
    path: docs/contracts/admission/admission-contract.json
    status: active
    owner: operations-runtime
    owner_domain: operations-runtime
    responsibility: Unified visible, prepare, confirm, and production decision contract.
    reason: surface visibility is not confirmAllowed; production remains blocked unless Admission and release-state allow it.
    allowed_usage: Evaluate visibleAllowed, prepareAllowed, confirmAllowed, productionAllowed, blocking sources, capabilities, device trust, and business-line state.
    forbidden_usage: Let Mobile surface, PC Governance, Workspace/Card, or Search decide confirmAllowed directly.
    evidence:
      - docs/contracts/admission/admission-contract.json
      - docs/contracts/admission/admission-matrix.json
      - services/core-api/WorkOS.Api/Runtime/AdmissionKernelService.cs
      - scripts/check-admission-kernel.mjs
  - id: OperationsRuntime
    name: Operations Runtime
    path: services/core-api/WorkOS.Api/Runtime
    status: active
    owner: operations-runtime
    owner_domain: operations-runtime
    responsibility: Primary execution axis and legal business write path.
    reason: Business facts must flow through WorkItem confirm, CommandSubmission, UoW, DomainEvent / LedgerEntry, ProcessManager, Projection / Lens, and Surface.
    allowed_usage: Own POST /api/operations/work-items/{workItemId}/confirm and commit through OperationsUnitOfWork.
    forbidden_usage: Accept page-specific business writes, shadow_runtime official reads, direct ProjectionRuntime confirm, or Mobile BFF fact writes.
    evidence:
      - services/core-api/WorkOS.Api/Runtime/CanonicalOperationsApiService.cs
      - services/core-api/WorkOS.Api/Runtime/OperationsUnitOfWork.cs
      - scripts/check-runtime-write-paths.mjs
      - scripts/check-api-boundaries.mjs
  - id: CompatibilityBox
    name: Compatibility Box
    path: docs/architecture/compatibility-components.yml
    status: active
    owner: architecture
    owner_domain: architecture
    responsibility: Register quarantined projection facades and enforce retired Workspace/Card write-route deletion without promoting them to architecture center.
    reason: ProjectionRuntime remains a quarantined projection facade; retired Workspace/Card write routes must stay deleted while Operations Runtime owns commands.
    allowed_usage: Read, display, map, and adapt non-authoritative legacy material through controlled boundaries.
    forbidden_usage: Add business write semantics, decide confirmAllowed, bypass Admission Kernel, or grant production.
    evidence:
      - docs/architecture/compatibility-components.yml
      - docs/architecture/compatibility-quarantine-rules.md
      - scripts/check-compatibility-quarantine.mjs
  - id: ExperienceKernel
    name: Experience Kernel
    path: apps/mobile/src/views
    status: active
    owner: surface
    owner_domain: surface
    responsibility: Present runtime decisions, blockers, required fields, evidence state, and recovery guidance without owning business facts.
    reason: UI/UX must express Operations Runtime decisions clearly while avoiding technical fields and hidden business writes.
    allowed_usage: Render Mobile / PC surfaces from Projection / Lens, Language Kernel copy, and Admission decisions.
    forbidden_usage: Display internal command fields to users, decide confirmAllowed, write business facts, or create page-specific write flows.
    evidence:
      - apps/mobile/src/views/operationPanelView.js
      - apps/mobile/src/views/workspaceView.js
      - apps/mobile/e2e/surface-smoke.spec.js
      - scripts/check-surface-contract.mjs
  - id: LanguageKernel
    name: Language Kernel
    path: docs/contracts/language/language-contract.json
    status: active
    owner: language
    owner_domain: language
    responsibility: Authoritative status, explanation, risk, field label, error, and search synonym language for zh-CN, ru-RU, and ky-KG.
    reason: Language must not be scattered as page-specific patches and must cover business states and admission explanations.
    allowed_usage: Provide catalogs and legacy adapters for domain terms, field labels, surface copy, error copy, and search synonyms.
    forbidden_usage: Let pages, cards, BFF, or Projection facade invent business state semantics independently.
    evidence:
      - docs/contracts/language/language-contract.json
      - docs/contracts/language/domain-term-catalog.json
      - docs/contracts/language/field-label-catalog.json
      - scripts/check-language-kernel.mjs
  - id: SearchKernel
    name: Search Kernel
    path: docs/contracts/search/search-contract.json
    status: active
    owner: search
    owner_domain: search
    responsibility: Read-only search with Admission, permission, ranking, language, and source evidence.
    reason: Search must not become contains-only routing or bypass permissions / language / admission.
    allowed_usage: Return authorized, language-aware, admission-labeled results and source references.
    forbidden_usage: Write business facts, bypass tenant/auth filters, bypass Admission, bypass Language Kernel, or expose hidden production actions.
    evidence:
      - docs/contracts/search/search-contract.json
      - services/core-api/WorkOS.Api/Runtime/SearchKernelService.cs
      - scripts/check-search-kernel.mjs
  - id: ControlPlane
    name: Control Plane
    path: tools/control-plane
    status: active
    owner: governance
    owner_domain: governance
    responsibility: GateResult, Invariant, ShadowCompare, RollbackInstruction, release evidence, and controlled governance actions.
    reason: Governance and release decisions need auditable evidence and must not directly write business facts.
    allowed_usage: Read and generate governance evidence, gate artifacts, rollback instructions, and shadow comparison outputs.
    forbidden_usage: Directly mutate business facts, bypass Operations Runtime, or claim Business Production GO.
    evidence:
      - artifacts/release-state/current-state.json
      - artifacts/rt4/evidence-graph.json
      - tools/control-plane/WorkOS.ControlPlaneRunners
      - scripts/operations/check-l1-to-l2-readiness.mjs
  - id: EvidenceGraph
    name: Evidence Graph
    path: artifacts/rt4/evidence-graph.json
    status: active
    owner: governance
    owner_domain: governance
    responsibility: Bind release evidence, operation evidence, surface evidence, gates, traces, and final reports.
    reason: OAM-CAB completion must be evidence-backed and machine-readable, not asserted by prose.
    allowed_usage: Record artifacts, screenshots, gate results, no-go risks, and traceability.
    forbidden_usage: Fabricate pass evidence, hide missing commands, or override current-state.
    evidence:
      - artifacts/rt4/evidence-graph.json
      - artifacts/oam-cab/oam-cab-v1-current-state.md
      - artifacts/oam-cab/execution-log.jsonl
  - id: DefinitionRegistry
    name: Definition Registry
    path: docs/contracts/definition/workitem-definition-registry.json
    status: active
    owner: operations-runtime
    owner_domain: operations-runtime
    responsibility: Authoritative WorkItem, command, field, evidence, risk, ledger, surface, and projection owner semantics.
    reason: New business semantics must enter Definition Registry rather than Workspace/Card extension points.
    allowed_usage: Resolve work item definitions before admission, search, field rendering, and runtime confirm.
    forbidden_usage: Keep business semantics only in page code, retired Workspace/Card write paths, or ad hoc surface copy.
    evidence:
      - docs/contracts/definition/workitem-definition-registry.json
      - services/core-api/WorkOS.Api/Runtime/WorkItemDefinitionRegistryService.cs
      - scripts/check-definition-registry.mjs
  - id: BusinessDomainKernel
    name: Business Domain Kernel
    path: docs/contracts/company-kernels/company-kernel-alignment-contract.json
    status: active
    owner: business-architecture
    owner_domain: business-architecture
    responsibility: Bind business lines, domain packs, maturity, and go/no-go domain semantics to OAM-CAB without creating business writes.
    reason: Company-level architecture must start from business reality and domain semantics before Definition and Admission.
    allowed_usage: Define domain packs, business-line maturity, domain object catalogs, and go/no-go readiness inputs.
    forbidden_usage: Commit center truth facts, commit finance ledger facts, or grant Business Production.
    evidence:
      - docs/contracts/company-kernels/company-kernel-alignment-contract.json
      - docs/business/domain-kit/domain-template.yml
      - scripts/check-company-kernel-alignment.mjs
      - scripts/check-business-domain-kit.mjs
  - id: MasterObjectKernel
    name: Master Object Kernel
    path: docs/business/shared-governance/subject-vehicle-truth.yml
    status: active
    owner: shared-governance
    owner_domain: shared-governance
    responsibility: Own Subject, Vehicle, FormalRelationship, master receipts, and master summaries.
    reason: Subject and vehicle master objects must be company-level truth, not per-domain or page-local data.
    allowed_usage: Accept intake, steward duplicates, merge/supersede master objects, and expose read-only summaries.
    forbidden_usage: Let domain packs, projections, management cockpit, or pages create formal master truth directly.
    evidence:
      - docs/business/shared-governance/subject-vehicle-truth.yml
      - docs/business/truth-owner-registry.yml
      - scripts/check-shared-governance-boundary.mjs
      - scripts/check-company-kernel-alignment.mjs
  - id: TruthBoundaryKernel
    name: Truth Boundary Kernel
    path: docs/business/truth-owner-registry.yml
    status: active
    owner: governance
    owner_domain: governance
    responsibility: Define truth owners, fact ownership, provisional reference policy, and receipt-only projection boundaries.
    reason: Company-level truth boundaries prevent projection, cockpit, or domain shortcuts from becoming official fact owners.
    allowed_usage: Decide who may commit each center truth fact and which references are draft or receipt-only.
    forbidden_usage: Allow shadow runtime, projection fallback, management cockpit, or mobile surface to own official business facts.
    evidence:
      - docs/business/truth-owner-registry.yml
      - docs/rules/v5.5/fact-ownership.yml
      - scripts/check-truth-owners.mjs
      - scripts/check-company-kernel-alignment.mjs
  - id: FinanceTruthKernel
    name: Finance Truth Kernel
    path: docs/business/finance/finance-truth-pipeline.yml
    status: active
    owner: finance
    owner_domain: finance
    responsibility: Route MoneyBasis through FinanceTruthPack and MoneyKernelPack before ledger facts are committed.
    reason: Payment, deposit, correction, and ledger truth must have one finance truth mouth and balanced ledger output.
    allowed_usage: Define finance intake, finance commit, ledger transaction, ledger entry, correction, and unclear money routing.
    forbidden_usage: Let business domain pages confirm payment/deposit facts directly or commit unbalanced ledger entries.
    evidence:
      - docs/business/finance/finance-truth-pipeline.yml
      - docs/business/finance/ledger-transaction-contract.yml
      - scripts/check-finance-truth.mjs
      - scripts/check-company-kernel-alignment.mjs
  - id: EventStateSummaryKernel
    name: Event-State-Summary Kernel
    path: docs/contracts/fact-trace.schema.json
    status: active
    owner: operations-runtime
    owner_domain: operations-runtime
    responsibility: Define DomainEvent, StateTransition, Summary, ProjectionCommit, and FactTrace scope.
    reason: Runtime summaries and projections must trace to committed events and ledger facts rather than surface-only state.
    allowed_usage: Define event/state/summary contracts, projection commit traceability, and source fact lineage.
    forbidden_usage: Invent untraced read-side summaries or mutate business facts through event summary contracts.
    evidence:
      - docs/contracts/fact-trace.schema.json
      - docs/contracts/projection-contract.schema.json
      - scripts/validate-runtime-api.mjs
      - scripts/check-company-kernel-alignment.mjs
  - id: BIKPIKernel
    name: BI/KPI Kernel
    path: docs/contracts/bi-kpi/bi-kpi-contract.json
    status: active
    owner: operations-analytics
    owner_domain: operations-analytics
    responsibility: Define KPI formulas, lineage, read-only dashboards, and metric deviation routing.
    reason: BI and dashboards must remain read-only and traceable, not hidden fact writers or production gate substitutes.
    allowed_usage: Provide metric definitions, dimensions, lineage, maturity scope, and feedback-loop triggers.
    forbidden_usage: Write business facts, manually override KPI truth, or grant production admission.
    evidence:
      - docs/contracts/bi-kpi/bi-kpi-contract.json
      - docs/business/dormitory/metric-formula-contract.yml
      - scripts/business/check-metric-formula-contract.mjs
      - scripts/check-company-kernel-alignment.mjs
  - id: AccountApplicationBoundaryKernel
    name: Account/Application Boundary Kernel
    path: docs/contracts/account-actor-kernel/account-actor-kernel-contract.json
    status: active
    owner: security-platform
    owner_domain: security-platform
    responsibility: Bind actor, device, permission, application/API boundary, and cutover policy into Admission.
    reason: Application surfaces and accounts must not bypass Admission, API boundary, or release-state.
    allowed_usage: Evaluate actor capability, device trust, API boundary, permission policy, and cutover constraints.
    forbidden_usage: Override confirmAllowed, write business facts, bypass API boundary, or grant Business Production.
    evidence:
      - docs/contracts/account-actor-kernel/account-actor-kernel-contract.json
      - docs/contracts/admission/actor-device-admission-contract.json
      - docs/business/policies/permission-policy.yml
      - scripts/check-account-actor-kernel.mjs
      - scripts/check-admission-kernel.mjs
      - scripts/check-company-kernel-alignment.mjs
